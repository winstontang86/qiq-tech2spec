---
name: qiq-tech2spec
description: 将技术方案 / RFC / 设计文档转换为字段级、流程级、错误码级精确的实现规格书。只产出 `<方案名>-spec.md` 与 `SPEC_COVERAGE.md`，不执行后续工程落地动作。触发：技术方案生成规格书 / 方案转规格书 / design doc to spec / tech2spec / 实现规格书生成。
version: 0.1.0
---

# qiq-tech2spec — 技术方案文档生成实现规格书

## When to use

满足任一条件就启用：

- 用户拿到一份技术方案 / RFC / 设计文档，希望把它转换成**可直接指导编码的实现规格书**。
- 用户提到关键词：**技术方案生成规格书 / 方案转规格书 / design doc to spec / tech2spec / 实现规格书生成**。
- 用户希望把方案中的模块、字段、接口、DDL、错误码、核心流程、异常路径、配置参数转成无歧义的落地说明。

不适用：

- 用户要求实际落地实现、运行测试、做代码审查或给出上线放行结论。
- 用户只想讨论方案是否合理，而不是产出规格书。
- 技术方案本身仍处于严重缺失或冲突状态，无法生成无歧义规格书；此时先澄清问题或建议回到方案评审。

## 只保留的环节

本 skill **只做一件事**：

```
技术方案文档 / RFC / Design Doc
        │
        ▼
实现规格书 `<方案名>-spec.md`
        +
覆盖率报告 `SPEC_COVERAGE.md`
```

除上述两个文档产物外，不执行任何工程落地动作，也不修改业务代码。

## 核心原则

1. **规格书是唯一产物核心**：本 skill 的目标是把方案语言翻译成实现语言，而不是开始实现。
2. **无歧义**：每个字段、类型、默认值、错误码、接口路径、状态转换、异常分支都必须明确。
3. **可追溯**：规格书每个关键条目都能追溯到原始方案章节；覆盖率报告必须列出映射关系。
4. **不擅自发挥**：禁止添加方案之外的功能、字段、错误码或流程；必要补充必须标注“规格书补充决策”与原因。
5. **按需观察仓库，不单独产出画像**：如需要沿用现有目录、命名或依赖风格，可以只读查看当前工作仓库并在规格书中写明证据。
6. **自检优先**：涉及 SQL / DB 时，必须按内置 SQL 自检规则先修正 MUST 违规；不把明显不合规内容交付给用户。

## 渐进披露阅读索引

主入口只放骨架，执行时按需读取以下文件：

| 用途 | 必读 reference | 必读 template |
|---|---|---|
| 规格书生成流程 | `references/01-spec-generation.md` | `templates/方案名-spec.md` |
| SQL / DB 自检 | `references/02-sql-self-check-rules.md` | `templates/方案名-spec.md` §2.1.1 |
| 编码约束来源 | `references/03-coding-constraints-common.md` + `references/04-coding-constraints-go.md` | `templates/方案名-spec.md` §9 |
| 覆盖率报告 | `references/01-spec-generation.md` | `templates/SPEC_COVERAGE.md` |

## Workflow

### Step 1 — 确认输入与产物位置

确认以下信息：

- 原始技术方案文档：用户粘贴的正文，或本地文件路径。
- 方案名：用于命名 `<方案名>-spec.md` 与 `.qiqskills/tech2spec/<方案名>/SPEC_COVERAGE.md`。
- 目标语言 / 技术栈：若方案或仓库未明确，先从方案提取；仍不明确时向用户确认。

默认产物位置：

- 规格书：优先写到**技术方案文档所在目录**下的 `<方案名>-spec.md`。
- 覆盖率报告：写到**工作仓库根目录**下的 `.qiqskills/tech2spec/<方案名>/SPEC_COVERAGE.md`。
- 若用户只粘贴方案正文且没有文档路径，则两个产物都写到 `.qiqskills/tech2spec/<方案名>/`。

### Step 2 — 读取方案并提取实现信息

按 `references/01-spec-generation.md` 执行信息提取，至少覆盖：

- 项目结构与改动文件清单。
- 数据存储：DDL、DML、查询 SQL、Redis key、一致性、演进策略。
- 核心数据结构：字段、类型、tag、枚举、状态机。
- 接口契约：HTTP / RPC / MQ、请求响应、错误码、幂等。
- 核心流程伪代码：步骤、异常分支、事务边界、回滚、并发约束。
- 边界与异常路径。
- 模块依赖关系。
- 配置、环境变量、监控指标。
- 编码约束清单。

如方案缺少生成规格书必需的信息，应优先从方案上下文和当前仓库只读观察中补齐；仍无法确定时，向用户提出聚焦的澄清问题，不编造。

### Step 3 — 生成实现规格书

按 `templates/方案名-spec.md` 的 **9 个主体章节 + 附录 A/B** 生成 `<方案名>-spec.md`。

硬性要求：

- 9 个主体章节全部存在且非空。
- §1.4 改动文件清单不为空。
- §2 数据存储详设不留空；不涉及 DB / Redis 时必须明确写“不涉及”及原因。
- §2.1.1 SQL 自检表必须完成；MUST 未解决项必须为 0。
- §5 核心流程伪代码覆盖方案中的关键流程；每段包含异常分支和事务 / 回滚说明。
- §6 边界与异常路径逐条映射方案中的故障 / 异常项。
- §8 配置与运行参数包含 config schema、环境变量映射、监控指标。
- §9 编码约束必须可机械验证，禁止“尽量”“建议”“按需”等含糊措辞。

### Step 4 — 生成覆盖率报告

按 `templates/SPEC_COVERAGE.md` 同步生成 `SPEC_COVERAGE.md`。

覆盖率报告必须逐条映射：

- 方案章节。
- 数据表、Redis key、存储演进项。
- Mermaid 时序图 / 流程图 / 状态机。
- 故障与异常路径。
- SLO、容量、监控、安全、降级、灰度等非功能项。

原则上**未覆盖项 = 0**。若确实无法覆盖，必须登记豁免理由、Owner 与后续处理方式。

### Step 5 — 自检与交付

交付前按以下清单自检：

- 规格书 9 个主体章节齐全且必填表格不为空。
- 所有方案存储项均在规格书 §2 落地。
- 所有接口均有请求、响应、错误码、鉴权、限流、幂等说明。
- 所有关键流程均有伪代码、异常分支、事务边界、回滚 / 补偿策略。
- 所有配置、环境变量、监控指标均在 §8 落地。
- 所有 SQL 已完成自检，MUST 未解决项 = 0。
- `SPEC_COVERAGE.md` 已产出，且未覆盖项为 0 或已显式豁免。

最后向用户输出简短交付摘要，只说明产物路径、覆盖结论、遗留待确认项。**交付后停止，不进入工程落地。**

## 产物目录约定

本 skill 只允许写以下产物：

```
<技术方案文档所在目录>/
└── <方案名>-spec.md              # 实现规格书；若无源文档路径则写入 .qiqskills/tech2spec/<方案名>/

.qiqskills/tech2spec/<方案名>/
└── SPEC_COVERAGE.md             # 方案 → 规格书覆盖率报告
```

禁止写入：

- 除 `<方案名>-spec.md` 与 `SPEC_COVERAGE.md` 外的运行产物。
- 任何业务代码、测试代码、配置代码或 SQL 文件（除非这些内容只是规格书中的代码块）。

## 红线

- ❌ **写代码**：本 skill 只生成文档产物，不修改业务代码。
- ❌ **继续落地**：不得把规格书继续推进为工程实施计划或实现过程。
- ❌ **冒充工程校验通过**：不得对业务实现给出构建 / 测试 / 上线放行结论。
- ❌ **规格书外发挥**：不得凭空添加方案没有的功能、字段、错误码、流程。
- ❌ **跳过 SQL 自检**：规格书包含 DDL / DML / 查询 SQL 时，必须完成 §2.1.1 自检。
- ❌ **留下未解释空表**：必填章节不适用时必须写“不涉及”与理由，不能留空。

## Pitfalls

- **把方案原文复制成规格书**：规格书必须是实现级翻译，不是搬运。
- **只写模块不写字段**：字段类型、默认值、索引、tag、校验规则都要明确。
- **只写正常路径**：异常分支、回滚、补偿、降级同样是规格书主体。
- **覆盖率报告走形式**：必须逐章逐项映射，不能只写“已覆盖”。
- **把工程落地内容带回来**：本 skill 仅保留规格书生成环节。

## Verification

交付前必须全部满足：

- [ ] 已读取技术方案全文或用户粘贴的完整需求。
- [ ] 已生成 `<方案名>-spec.md`。
- [ ] 已生成 `.qiqskills/tech2spec/<方案名>/SPEC_COVERAGE.md`。
- [ ] 规格书 9 个主体章节全部存在且非空，附录 A/B 已填写。
- [ ] §1.4 改动文件清单至少 1 行，且不是示意占位。
- [ ] §2.1.1 SQL 自检完成；MUST 未解决项 = 0。
- [ ] §5 核心流程伪代码覆盖所有关键流程。
- [ ] §6 边界与异常路径覆盖方案故障 / 异常项。
- [ ] §8 配置、环境变量、监控指标齐全。
- [ ] §9 编码约束条款可机械验证。
- [ ] `SPEC_COVERAGE.md` 未覆盖项 = 0，或所有未覆盖项均已显式豁免。
- [ ] 未创建除 `<方案名>-spec.md` 与 `SPEC_COVERAGE.md` 之外的运行产物，且未修改业务代码。
