---
name: qiq-tech2spec
description: 将技术方案 / RFC / 设计文档转换为字段级、流程级、错误码级精确的实现规格书。只产出 `<方案名>-spec.md` 与 `<方案名>-speccover.md`，不执行后续工程落地动作。触发：技术方案生成规格书 / 方案转规格书 / design doc to spec / tech2spec / 实现规格书生成。
version: 1.2.2
---

# qiq-tech2spec — 技术方案文档生成实现规格书

## When to use

满足任一条件即启用：

- 用户拿到一份技术方案 / RFC / 设计文档，希望转成**可直接指导编码的实现规格书**。
- 用户提到关键词：**技术方案生成规格书 / 方案转规格书 / design doc to spec / tech2spec / 实现规格书生成**。
- 用户希望把方案中的模块、字段、接口、DDL、错误码、核心流程、异常路径、配置参数转成无歧义的落地说明。

不适用：

- 要求实际落地实现、运行测试、做代码审查或给出上线放行结论。
- 只想讨论方案是否合理，而非产出规格书。
- 方案本身严重缺失或冲突，无法生成无歧义规格书；此时先澄清或建议回到方案评审。

## 唯一职责

```
技术方案 / RFC / Design Doc
        │
        ▼
<方案名>-spec.md  +  <方案名>-speccover.md
```

除上述两个文档外，不执行任何工程落地动作，也不修改业务代码。

## 核心原则

1. **翻译而非搬运**：把方案语言翻译成实现语言，不是开始实现，也不是复制原文。
2. **无歧义**：每个字段、类型、默认值、错误码、接口、状态转换、异常分支都必须明确。
3. **可追溯**：规格书每个关键条目都能追溯到原始方案章节，覆盖率报告列出映射关系。
4. **不擅自发挥**：禁止添加方案之外的功能、字段、错误码或流程；必要补充须标注"规格书补充决策"与原因。
5. **按需观察仓库**：可只读查看当前工作仓库以沿用现有目录、命名或依赖风格，并在规格书中写明证据；不单独产出仓库画像。
6. **SQL 自检前置**：涉及 SQL / DB 时，先按内置规则修正 MUST 违规，不把不合规内容交付用户。
7. **最简充分方案 + 可扩展**：在能完整满足方案中**功能需求与非功能需求（性能 / 可用性 / 一致性 / 安全 / 容量 / SLO 等）**的前提下，规格书选取**最简单的实现路径**——更少的组件、更少的中间件、更少的状态、更少的并发原语、更少的抽象层级；同时保留**沿扩展轴的演进口子**（接口边界、配置项、表结构演进策略），但**不**预先实现尚未需要的能力。每一处偏离“最简”的设计，必须在规格书 §10 简洁性与可扩展性自检中显式登记“为什么不能更简单 / 满足了哪条非功能需求”。
8. **闭环交付（无 TODO）**：方案中明确要求的每个功能、字段、错误码、流程、异常路径，规格书必须**当场闭环**——给出可直接编码的决策；**不允许**出现 `TODO` / `FIXME` / `待补充` / `后续 PR 实现` / `下一期再做` / `留待工程师决定` / `先这样后面再优化` 这类延迟入口。所有需人工拍板的参数与信息缺口，必须在 **Step 0 执行前预检门禁**向用户澄清并确认为定案后再落入规格书正文；正文不设“待确认”出口，也不再保留“待用户确认”附录。
9. **按需实现，不外溢**：方案没有提出的功能、非功能需求、SLO、容量、扩展点，规格书一律不实现、不预留代码、不预留字段、不预留枚举、不预留错误码、不预留接口、不预留配置项、不预留中间件。可扩展轴只在 §10 以“接口契约 / 配置开关 / DDL expand-migrate-contract / 错误码空间”四种轻量形式**声明**，**不允许**在本版本写出对应实现或占位代码。

## 渐进披露阅读索引

主入口只放骨架，执行时按需读取：

| 用途 | 必读 reference |
|---|---|
| 规格书生成流程 / 覆盖率报告 | `references/01-spec-generation.md`（配合 `templates/方案名-spec.md` 与 `templates/方案名-speccover.md`） |
| SQL / DB 自检 | `references/02-sql-self-check-rules.md`（落地到规格书 §2.1.1） |
| 编码约束来源 | `references/03-coding-constraints-common.md` + `references/04-coding-constraints-go.md`（落地到规格书 §9） |

## 产物目录约定

最终交付产物为**两份强绑定文档**：实现规格书与覆盖率报告，二者**同目录、成对交付**，均与原始技术方案文档放在同一目录，以 `<方案名>` 为唯一锚点。

```
<技术方案文档所在目录>/
├── <方案名>-spec.md              # 实现规格书（最终交付）
└── <方案名>-speccover.md         # 方案 → 规格书覆盖率报告（与规格书同目录、成对交付）
```

约定：

- `<方案名>-spec.md` 与 `<方案名>-speccover.md` 必须与原始技术方案文档**同目录**，文件名前缀 `<方案名>` 逐字一致；下游只需一个 `<方案名>` 即可机械推出两条路径。迁移 / 清理时二者必须成对处理。
- 用户只粘贴方案正文、未提供文档路径时，两份产物一并落到 `.qiqskills/<方案名>/`（仍保持同目录、同前缀）。
- 不允许写入上述位置之外的任何运行产物（业务代码、测试代码、配置代码、独立 SQL 文件等）。规格书内部的代码块不算独立产物。

## Workflow

### Step 0 — 执行前预检门禁（**强制前置，未通过不得进入 Step 1**）

本 skill 假设输入的 tech/prd 是**已明确、彼此一致**的方案。生成规格书前，必须先过两道门禁：

**门禁 A — 上游矛盾预检（阻断式）**

- 读取 prd 与 tech，检查三类矛盾：① prd ↔ tech 之间；② tech 内部；③ prd 内部。
- 重点排查字段级 / 类型级 / 错误码级 / 流程级 / 约束级的**硬矛盾**（例：幂等键取 `(session_id, seq)` 去重，但 `seq` 又设计为服务端自增；`content` 要求 256KB 却声明为 `TEXT`；同一错误场景两处错误码不一致；某流程在 prd 与 tech 中步骤 / 状态相互冲突）。
- **一旦发现硬矛盾即停止执行**，向用户输出矛盾清单（矛盾点 / prd 出处 / tech 出处 / 冲突说明 / 建议方向），要求用户先在 **tech/prd 层面人工修正并统一**后重新触发本 skill。本 skill **不擅自修改 tech/prd**，也不在规格书里单方面裁决绕过矛盾。
- 未发现硬矛盾则记录“上游一致性预检通过”，进入门禁 B。

**门禁 B — 待确认项预检（确认式）**

- 列出所有需人工拍板、无法从方案 + 仓库只读证据 + 通用默认值确定性推导的关键项（业务阈值、容量口径、安全合规选型、授权人、外部依赖契约细节等）。
- 每项**给出推荐方案**（含理由），逐项呈现给用户。
- **所有待确认项必须经用户确认后**才能继续；用户确认结果即视为定案，直接写入规格书正文对应章节。
- 若无待确认项，记录“无待确认项”后继续。

> 通过门禁 A、B 后方可进入 Step 1。经此门禁，进入生成阶段的输入已一致且无未决项，规格书正文因此**不再保留“待确认”附录**。

### Step 1 — 确认输入与产物位置

- **原始方案**：用户粘贴的正文，或本地文件路径。
- **方案名**：用于命名 `<方案名>-spec.md` 与同目录的 `<方案名>-speccover.md`，产物路径遵循上文"产物目录约定"。
- **目标语言 / 技术栈**：方案或仓库未明确时向用户确认。

### Step 2 — 读取方案并提取实现信息

按 `references/01-spec-generation.md` 提取，至少覆盖：

- 项目结构与改动文件清单。
- 数据存储：DDL、DML、查询 SQL、Redis key、一致性、演进策略。
- 核心数据结构：字段、类型、tag、枚举、状态机。
- 接口契约：HTTP / RPC / MQ、请求响应、错误码、幂等。
- 核心流程伪代码：步骤、异常分支、事务边界、回滚、并发约束。
- 边界与异常路径。
- 模块依赖关系。
- 配置、环境变量、监控指标。
- 编码约束清单。

方案缺失关键信息时，优先从方案上下文与仓库只读观察补齐；仍无法确定再向用户提聚焦的澄清问题，**不编造**。

### Step 3 — 生成实现规格书

按 `templates/方案名-spec.md` 生成 `<方案名>-spec.md`，必须满足：

- 10 个主体章节 + 附录 A 全部存在且非空；不适用章节须显式写"不涉及"及理由，不留空。
- §1.4 改动文件清单至少 1 行实质内容（非占位）。
- §2 数据存储详设落地；§2.1.1 SQL 自检表完成，**MUST 未解决项 = 0**。
- §3 数据结构字段表精确到类型 / 默认值 / tag / 索引；§4 接口契约含请求、响应、错误码、鉴权、限流、幂等。
- §5 核心流程伪代码覆盖所有关键流程，每段含异常分支与事务 / 回滚说明。
- §6 边界与异常路径逐条映射方案中的故障 / 异常项；§7 模块依赖图按 Layer 排序、无环。
- §8 含 config schema、环境变量映射、监控指标。
- §9 编码约束可机械验证，禁止"尽量""建议""按需"等含糊措辞。

### Step 4 — 生成覆盖率报告

按 `templates/方案名-speccover.md` 同步生成 `<方案名>-speccover.md`（与规格书同目录），逐条映射：

- 方案章节、数据表 / Redis key / 存储演进项。
- Mermaid 时序图 / 流程图 / 状态机。
- 故障与异常路径。
- SLO、容量、监控、安全、降级、灰度等非功能项。

覆盖率分母采用**机械计数口径**（编号叶子小节 / 表 / Redis key / 接口方法 / 错误码 / mermaid 图 / 故障行 / SLO 各计 1），填 §1.2 分类计数表，使 `N` 可复算。**未覆盖项 = 0**；确实无法覆盖的项必须登记豁免理由、Owner 与重启评估条件。

### Step 5 — 自检与交付

按本文末尾 [Verification](#verification) 清单逐项核对通过后，向用户输出简短交付摘要：两份产物路径、覆盖结论、SQL 自检结论、执行前预检结论（上游无矛盾、待确认项已确认）。**交付后停止，不进入工程落地。**

## 红线

- ❌ 写业务代码、测试代码、配置代码或独立 SQL 文件。
- ❌ 把规格书继续推进为工程实施计划或实现过程。
- ❌ 对业务实现给出构建 / 测试 / 上线放行结论。
- ❌ 凭空添加方案没有的功能、字段、错误码、流程。
- ❌ 跳过 §2.1.1 SQL 自检或留下 MUST 未解决项。
- ❌ 必填章节留空而不写"不涉及"与理由。
- ❌ 在规格书任何位置出现 `TODO` / `FIXME` / `待补充` / `待定` / `后续 PR` / `下一期` / `留待工程师决定` / `先这样` 等延迟决策入口；方案明确要求的功能必须在本规格书内闭环。
- ❌ 引入方案未提出需求所对应的组件、中间件、抽象层、并发原语、预留字段 / 枚举 / 错误码 / 接口 / 配置项；可扩展性仅以 §10 中的“轴声明”形式存在，禁止落到代码与表结构。

## Pitfalls

- **只写模块不写字段**：字段类型、默认值、索引、tag、校验规则都要明确。
- **只写正常路径**：异常分支、回滚、补偿、降级同样是规格书主体。
- **类型含糊**：写 `int64` / `time.Time` 等精确类型，禁止只写 `int` / `时间`。
- **覆盖率走形式**：必须逐章逐项映射，不能只写"已覆盖"。

## Verification

交付前必须全部满足：

- [ ] 已读取技术方案全文或用户粘贴的完整需求。
- [ ] 已完成 Step 0 执行前预检门禁：上游 tech/prd 无自相矛盾（如有已由用户人工修正），待确认项已逐项经用户确认。
- [ ] 已生成同目录、同前缀的 `<方案名>-spec.md` 与 `<方案名>-speccover.md`。
- [ ] 规格书 10 个主体章节 + 附录 A 全部存在且非空。
- [ ] §1.4 改动文件清单至少 1 行实质内容。
- [ ] §2.1.1 SQL 自检完成，MUST 未解决项 = 0。
- [ ] §3 字段表 / §4 接口契约（含错误码表）齐全。
- [ ] §5 伪代码覆盖所有关键流程；§6 覆盖方案故障 / 异常项；§7 模块依赖图按 Layer 排序、无环。
- [ ] §8 配置 schema / 环境变量 / 监控指标齐全；§9 编码约束条款可机械验证。
- [ ] §10 简洁性与可扩展性自检表已填写：每个引入的组件 / 中间件 / 抽象层 / 并发原语都对应明确的功能或非功能需求；YAGNI 自检清单逐项通过；未出现“为未来预留”代码或字段。
- [ ] **闭环检查（grep 全文）**：规格书全文不出现 `TODO`/`FIXME`/`待补`/`待定`/`后续 PR`/`下一期`/`留待`/`先这样` 等延迟决策措辞；方案中明确要求的功能、字段、错误码、流程、异常路径均已在本规格书内给出可直接编码的决策。
- [ ] **按需检查**：规格书未引入方案未要求的组件 / 中间件 / 抽象层 / 并发原语 / 预留字段 / 预留枚举 / 预留错误码 / 预留接口 / 预留配置项；§10 复杂度驱动表能反向追溯到原方案具体章节。
- [ ] **交叉引用可解析**：交付前人工逐一核对规格书与覆盖率报告中所有 `§x.y` 内部引用、以及对原方案的 `§x.y` 引用，确认目标章节真实存在。
- [ ] `<方案名>-speccover.md` §1.2 机械计数表已填写、`N` 可复算，未覆盖项 = 0，或全部已显式豁免（豁免类型仅限“风险接受”或“方案显式不实现”，**不允许**“后续迭代 / 后续 PR” 类豁免）。
- [ ] 未创建产物目录约定外的任何运行产物，且未修改业务代码。
